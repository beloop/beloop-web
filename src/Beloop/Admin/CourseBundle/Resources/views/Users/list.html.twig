<div class="dataTables_wrapper form-inline dt-bootstrap no-footer">
    <div class="row am-datatable-body">
        <div class="col-sm-12">
            <table class="table table-striped table-fw-widget table-hover">
                {% include 'AdminCommonBundle:Common:Tables/head.html.twig' with {
                    rows: [
                        { name: 'admin.user.field.full_name.title'|trans, width: 50 },
                        { name: 'admin.user.field.email.title'|trans, width: 40 },
                        { name: 'admin.common.field.actions.title'|trans, width: 10 },
                    ],
                } %}
                <tbody class="no-border-x">
                {% for user in users%}
                    <tr>
                        <td>{{ user.fullName }}</td>
                        <td>{{ user.email }}</td>
                        <td>
                            <div class="btn-toolbar">
                                <div class="btn-group btn-space">
                                </div>
                            </div>
                        </td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>
        </div>
    </div>
</div>